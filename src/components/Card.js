import styled from "styled-components";

const Card = styled.div`
  display: flex;
  height: 50vh;
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
  transition: 0.3s;
  padding: 25px;
  margin: 10px;
`;

export default Card;
